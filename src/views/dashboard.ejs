<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>

    <p>
        Name:
        <%= name %>
    </p>

    <p>
        Email:
        <%= email %>
    </p>

    <% if (roleId === 2) { %>
        <p>
            <a href="/users">
                View Registered Users
            </a>
        </p>
    <% } %>
</main>

<%- include('partials/footer') %>